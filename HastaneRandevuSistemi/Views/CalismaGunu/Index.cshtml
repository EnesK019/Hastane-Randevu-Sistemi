@model IEnumerable<HastaneRandevuSistemi.Models.CalismaGunu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Gunler)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Saatler)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DoktorId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Gunler)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Saatler)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DoktorId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CalismaGunID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CalismaGunID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CalismaGunID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
