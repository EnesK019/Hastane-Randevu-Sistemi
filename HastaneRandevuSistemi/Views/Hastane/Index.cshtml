@model IEnumerable<Hastane_Randevu_Sistemi.Models.Hastane>
<link rel="stylesheet" href="~/css/index.css" />
@{
	ViewData["Title"] = "Index";

}
<table class="table table-responsive-lg">
	<thead>
		<tr>
			<th>
				<h3 class="text-center">Hastaneler</h3>
			</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					<div class="row">
						<div class="col-lg-10">
							@Html.DisplayFor(modelItem => item.HastaneAdi)
						</div>
							<div class="col-lg-2">
							@if (User.IsInRole("Admin"))
							{
								<a asp-action="Details" asp-route-id="@item.HastaneID"><img class="icon" src="~/images/detail.png" /></a>
								<a asp-action="Edit" asp-route-id="@item.HastaneID"><img class="icon" src="~/images/düzenle.jpg" /></a>
								<a asp-action="Delete" asp-route-id="@item.HastaneID"><img class="icon" src="~/images/carpi.png" /></a>
							}
							else
							{
								<a asp-action="Details" asp-route-id="@item.HastaneID"><img class="icon" src="~/images/detail.png" /></a>
							}
						</div>
					</div>
				</td>
			</tr>
		}
	</tbody>
</table> 
<div class="text-center
	<a asp-action="Create" asp-route-id=""><img style="width: 45px; height: 45px;" src="~/images/arti.jpg" /></a>
</div>




@* <h3>Api den çekilen datalar</h3>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.HastaneID)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.HastaneAdi)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.HastaneID)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.HastaneAdi)
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.HastaneID">Edit</a> |
					<a asp-action="Details" asp-route-id="@item.HastaneID">Details</a> |
					<a asp-action="Delete" asp-route-id="@item.HastaneID">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table> *@